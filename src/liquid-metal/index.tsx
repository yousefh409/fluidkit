/** Placeholder — Task 2 replaces this with the real `@paper-design/shaders-react` wrapper. */

import type { HTMLAttributes } from "react";

export interface LiquidMetalProps extends HTMLAttributes<HTMLDivElement> {
  /** Shader colors. */
  colors?: string[];
  /** Animation speed multiplier. */
  speed?: number;
}

export function LiquidMetal(_props: LiquidMetalProps): never {
  throw new Error("fluidkit/liquid-metal: not yet implemented");
}
